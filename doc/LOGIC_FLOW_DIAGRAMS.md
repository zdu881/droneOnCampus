# 🔄 应用逻辑流程图 (详细版)

## 1️⃣ 应用启动流程 (初始化)

```
┌─────────────────────────────────────────────────────────┐
│ npm run start                                           │
│ 或双击 .exe 文件                                        │
└────────────────┬────────────────────────────────────────┘
                 ↓
        ┌────────────────┐
        │ Electron 初始化 │
        │ (app.ready)    │
        └────────┬───────┘
                 ↓
    ┌────────────────────────┐
    │ startCorsProxy()       │
    │ 启动 CORS 代理服务    │
    │ localhost:3000         │
    └────────┬───────────────┘
             ↓
    ┌────────────────────────┐
    │ new BrowserWindow()    │
    │ 创建应用窗口           │
    │ 1920x1080              │
    └────────┬───────────────┘
             ↓
    ┌────────────────────────┐
    │ loadFile('index.html') │
    │ 加载前端界面           │
    └────────┬───────────────┘
             ↓
    ┌────────────────────────┐
    │ initializeMonitoring() │
    │ 初始化监控组件         │
    └────────┬───────────────┘
             ├─────────────────────────────┐
             ↓                             ↓
   ┌──────────────────┐       ┌──────────────────┐
   │ DroneFlightMonitor│       │PixelStreamManager│
   │ (飞行监控器)     │       │(流管理器)        │
   │ serverUrl: 8000  │       │streamUrl: 80     │
   └────────┬─────────┘       └────────┬─────────┘
            ↓                          ↓
   ┌──────────────────┐       ┌──────────────────┐
   │ start()          │       │ (已就绪等待)     │
   │ 开始轮询         │       │                  │
   └────────┬─────────┘       └──────────────────┘
            ↓
   ┌──────────────────────┐
   │ ✅ 应用启动完成      │
   │ 等待飞行信号        │
   └──────────────────────┘
```

---

## 2️⃣ 飞行监控轮询循环

```
                    ┌──────────────────────┐
                    │  启动监控轮询        │
                    │  间隔: 500ms         │
                    └──────────┬───────────┘
                               │
         ┌─────────────────────┴─────────────────────┐
         ↓                                           ↓
    ┌─────────────────┐                 ┌───────────────────┐
    │ 发送 HTTP 请求  │                 │ 等待 500ms        │
    │ GET /api/       │                 │ (之前轮询)        │
    │ drone/status    │                 └────┬──────────────┘
    │                 │                      │
    │ 超时: 5000ms    │                      ↓
    └────────┬────────┘              ┌─────────────────┐
             │                       │ 轮询再次触发    │
             ↓                       └────────┬────────┘
    ┌─────────────────┐                      │
    │ 收到响应?       │ ◄──────────────────┘
    └─┬───────────┬──┘
      │           │
      │ 否        │ 是
      ↓           ↓
    ┌───────┐  ┌──────────────────────┐
    │重试   │  │ 解析 JSON            │
    │递增   │  │ data.isFlying = ?    │
    │     │  └──┬───────────────────┘
    │     │     │
    │     │  ┌──┴──────────────────────┐
    │     │  │ 当前是否在飞行?         │
    │     │  └──┬────────────┬────────┘
    │     │     │            │
    │     │ 是飞行        不飞行
    │     │     │            │
    │     │     ↓            ↓
    │     │  ┌──────┐    ┌──────┐
    │     │  │从停止│    │从飞行│
    │     │  │变为  │    │变为  │
    │     │  │飞行? │    │停止? │
    │     │  └──┬───┘    └───┬──┘
    │     │     │ 是         │ 是
    │     │     ↓            ↓
    │     │  ┌──────────┐ ┌─────────┐
    │     │  │触发事件  │ │触发事件 │
    │     │  │flight:   │ │flight:  │
    │     │  │started   │ │stopped  │
    │     │  └──┬───────┘ └────┬────┘
    │     │     │              │
    │     │  ┌──┴──────────────┴──┐
    │     │  │ 处理状态变化      │
    │     │  │ (见下一个流程)    │
    │     │  └──────┬─────────────┘
    │     │         │
    │     └─┬───────┘
    │       ↓
    │  ┌─────────────────────┐
    │  │ 重置重试计数器      │
    │  │ retryCount = 0      │
    │  └─────┬───────────────┘
    │        │
    └────────┘
```

---

## 3️⃣ 飞行开始事件处理 (flight:started)

```
    ┌─────────────────────────────────────┐
    │ droneMonitor 检测飞行开始           │
    │ isFlying: false → true              │
    └──────────────┬──────────────────────┘
                   ↓
        ┌──────────────────────┐
        │ 触发 'flight:started' │
        │ 事件                  │
        └──────────┬───────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ main.js 中的监听器被调用:            │
    │ droneMonitor.on('flight:started')    │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 1: 启动像素流                   │
    │ streamManager.startStream()          │
    │  ↓                                   │
    │  - 设置 isActive = true              │
    │  - 记录启动时间                     │
    │  - 发送 'stream:started' 事件       │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 2: 通知前端更新状态              │
    │ mainWindow.webContents.send(        │
    │   'stream:status',                   │
    │   {                                  │
    │     status: 'streaming',             │
    │     message: '正在接收像素流...',    │
    │     timestamp: Date.now()            │
    │   }                                  │
    │ )                                    │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 3: 前端接收消息                  │
    │ window.electronAPI.onStreamStatus()  │
    │  ↓                                   │
    │  - updateStatus('streaming')         │
    │  - 改变状态灯颜色为绿色              │
    │  - 更新状态文本                     │
    │  - 调用 startDisplayingStream()     │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 4: 显示像素流                    │
    │ startDisplayingStream()               │
    │  ↓                                   │
    │  - 创建 <iframe> 元素                │
    │  - 设置 src = "http://10.30.2.11:80"│
    │  - 注入到 DOM 中                     │
    │  - iframe 开始加载视频流             │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 5: 记录日志                      │
    │ logElement.append(                   │
    │   '🎬 像素流已启动'                   │
    │ )                                    │
    └──────────────┬───────────────────────┘
                   ↓
        ┌──────────────────────┐
        │ ✅ 开始显示流         │
        │ 显示无人机实时视频   │
        └──────────────────────┘
```

---

## 4️⃣ 飞行停止事件处理 (flight:stopped)

```
    ┌──────────────────────────────────────┐
    │ droneMonitor 检测飞行停止            │
    │ isFlying: true → false               │
    └──────────────┬───────────────────────┘
                   ↓
        ┌──────────────────────┐
        │ 触发 'flight:stopped' │
        │ 事件                  │
        └──────────┬───────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ main.js 中的监听器被调用:            │
    │ droneMonitor.on('flight:stopped')    │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 1: 停止像素流                    │
    │ streamManager.stopStream()           │
    │  ↓                                   │
    │  - 设置 isActive = false             │
    │  - 计算运行时间 = 现在 - 启动时间   │
    │  - 发送 'stream:stopped' 事件       │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 2: 通知前端更新状态              │
    │ mainWindow.webContents.send(        │
    │   'stream:status',                   │
    │   {                                  │
    │     status: 'idle',                  │
    │     message: '等待无人机飞行...',    │
    │     timestamp: Date.now()            │
    │   }                                  │
    │ )                                    │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 3: 前端接收消息                  │
    │ window.electronAPI.onStreamStatus()  │
    │  ↓                                   │
    │  - updateStatus('idle')              │
    │  - 改变状态灯颜色为红色              │
    │  - 更新状态文本                     │
    │  - 调用 stopDisplayingStream()      │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 4: 清除像素流                    │
    │ stopDisplayingStream()                │
    │  ↓                                   │
    │  - 清空 streamContainer              │
    │  - 显示占位符                       │
    │  - 显示停止图标                     │
    │  - 显示提示文本                     │
    └──────────────┬───────────────────────┘
                   ↓
    ┌──────────────────────────────────────┐
    │ 步骤 5: 记录日志                      │
    │ logElement.append(                   │
    │   '⏹️ 像素流已停止'                   │
    │ )                                    │
    └──────────────┬───────────────────────┘
                   ↓
        ┌──────────────────────┐
        │ ✅ 流已停止          │
        │ 等待下次飞行        │
        └──────────────────────┘
```

---

## 5️⃣ 手动控制流程 (UI 按钮)

### 手动启动流:

```
用户点击 "启动流" 按钮
    ↓
<button id="start-btn">启动流</button>.click()
    ↓
window.electronAPI.startStream() [IPC 消息]
    ↓
ipcMain.on('stream:start', () => {
  streamManager.startStream();
  mainWindow.webContents.send('stream:status', {...});
})
    ↓
前端收到消息，立即显示流
    ↓
iframe 加载视频
```

### 手动停止流:

```
用户点击 "停止流" 按钮
    ↓
<button id="stop-btn">停止流</button>.click()
    ↓
window.electronAPI.stopStream() [IPC 消息]
    ↓
ipcMain.on('stream:stop', () => {
  streamManager.stopStream();
  mainWindow.webContents.send('stream:status', {...});
})
    ↓
前端收到消息，立即清除流
    ↓
显示占位符和停止图标
```

---

## 6️⃣ 配置更新流程

```
用户输入新配置信息
    ↓
编辑框中输入 Dashboard URL 或 Stream URL
    ↓
点击 "保存配置" 按钮
    ↓
window.electronAPI.updateConfig({
  dashboardUrl: 'http://...',
  streamUrl: 'http://...'
})
    ↓
ipcMain.on('config:update', (event, config) => {
  if (config.dashboardUrl) {
    droneMonitor.serverUrl = config.dashboardUrl;
  }
  if (config.streamUrl) {
    streamManager.streamUrl = config.streamUrl;
  }
  event.reply('config:updated', { success: true });
})
    ↓
前端收到确认
    ↓
显示 "✅ 配置已保存" 提示
    ↓
✅ 新配置立即生效
   (下一次轮询使用新的 URL)
```

---

## 7️⃣ 完整时间线示例 (真实场景)

```
时间      日志信息                              状态           UI 显示
──────────────────────────────────────────────────────────────────────
00:00     🚀 应用已启动                        初始化         红灯 + 等待
00:00     🌐 CORS proxy server started         初始化         -
00:00     🎯 Starting flight monitor           初始化         -
00:05     ✅ 应用加载完成                      就绪           红灯 + 等待
00:10     (轮询检查中...)                      等待           -
00:15     (轮询检查中...)                      等待           -
00:20     (轮询检查中...)                      等待           -
00:25     (轮询检查中...)                      等待           -

[用户起飞无人机]

00:26     ✈️ DRONE FLIGHT STARTED              飞行中         -
00:26     🎬 Starting pixel stream             启动流         -
00:26     📊 状态: streaming - ...             流启动         绿灯 + iframe
00:26     [iframe 加载视频]                    显示流         [实时视频]
00:27     (轮询检查中...)                      流接收         [实时视频]
00:28     (轮询检查中...)                      流接收         [实时视频]
01:00     (轮询检查中...)                      流接收         [实时视频]
05:00     (轮询检查中...)                      流接收         [实时视频]

[用户操作无人机，持续飞行]

10:00     (轮询检查中...)                      流接收         [实时视频]

[用户着陆无人机]

10:05     🛬 DRONE FLIGHT STOPPED              停止飞行       -
10:05     ⏹️ Stopping pixel stream             停止流         -
10:05     ⏱️ Stream duration: 600s             时长计算       -
10:05     📊 状态: idle - ...                  流停止         红灯 + 占位符
10:05     [清除 iframe]                        清除流         [停止图标]
10:06     (轮询检查中...)                      等待           红灯 + 等待
10:10     (轮询检查中...)                      等待           -
10:15     (轮询检查中...)                      等待           -

[应用继续运行，等待下一次飞行]
```

---

## 8️⃣ 关键的状态转换矩阵

```
当前状态         检测到的信号           下一步行动              结果状态
─────────────────────────────────────────────────────────────────────
idle             飞行开始信号           → startStream()        streaming
(等待)           (isFlying=false→true)  → 通知 UI              (接收)

streaming        飞行停止信号           → stopStream()         idle
(接收)           (isFlying=true→false)  → 通知 UI              (等待)

idle             用户点 "启动" 按钮     → startStream()        streaming
(等待)           (手动命令)             → 通知 UI              (接收)

streaming        用户点 "停止" 按钮     → stopStream()         idle
(接收)           (手动命令)             → 通知 UI              (等待)

ANY              网络错误               → 重试连接             维持原状
                 (连接失败)             → 显示错误             或 error

ANY              重试超过 3 次           → 发出错误事件        error
                 (完全断连)             → 显示警告信息        (错误)
```

---

## 9️⃣ 数据流向图

```
┌─────────────────────────────────────┐
│     10.30.2.11:8000 Dashboard API   │
│     (飞行状态查询)                   │
└──────────────┬──────────────────────┘
               │
               │ HTTP GET /api/drone/status
               │ 响应: { isFlying: boolean }
               ↓
        ┌──────────────┐
        │ CORS 代理    │
        │ :3000        │
        └──────┬───────┘
               │ (添加 CORS 头)
               ↓
   ┌───────────────────────────┐
   │ DroneFlightMonitor        │
   │ (轮询检查)                 │
   │ 500ms 间隔                 │
   └───────────┬───────────────┘
               │ 状态变化事件
        ┌──────┴──────┐
        ↓             ↓
   flight:started  flight:stopped
        │             │
        ├─────────────┤
        ↓
   ┌──────────────────────────┐
   │ main.js 事件处理         │
   │ 触发对应逻辑             │
   └───────────┬──────────────┘
               │
        ┌──────┴──────┐
        ↓             ↓
   ┌─────────────┐ ┌──────────────┐
   │ startStream │ │ stopStream   │
   └──────┬──────┘ └──────┬───────┘
          │               │
          └───────┬───────┘
                  ↓
   ┌──────────────────────────┐
   │ mainWindow.webContents   │
   │ .send('stream:status')   │
   │ 通知渲染进程             │
   └───────────┬──────────────┘
               │ IPC 消息
               ↓
   ┌──────────────────────────┐
   │ 前端 (renderer.js)       │
   │ 接收状态更新             │
   └───────────┬──────────────┘
               │
        ┌──────┴──────┐
        ↓             ↓
   updateStatus()  showUI()
        │             │
        └───────┬─────┘
                ↓
   ┌──────────────────────────┐
   │ 更新 UI 显示             │
   │ - 改变灯光颜色           │
   │ - 显示/隐藏 iframe       │
   │ - 更新日志               │
   └──────────────────────────┘


同时:
┌─────────────────────────────────────┐
│     10.30.2.11:80 像素流服务        │
│     (提供实时视频)                   │
└──────────────┬──────────────────────┘
               │
               │ iframe src 指向此服务
               ↓
        ┌──────────────┐
        │ <iframe>     │
        │ 直接加载     │
        └──────┬───────┘
               │
               ↓
   ┌──────────────────────────┐
   │ 在浏览器中显示视频流     │
   │ (UI 中的实时画面)        │
   └──────────────────────────┘
```

---

## 🔟 错误处理决策树

```
网络请求发生
    ↓
    └─→ 响应正常?
        ├─ YES → 解析 JSON 数据
        │        ↓
        │        ├─ 格式正常?
        │        │  ├─ YES → 提取 isFlying
        │        │  └─ NO  → 记录错误，重试
        │        └─ (继续)
        │
        └─ NO  → 网络错误
                 ↓
                 ├─ HTTP 超时?
                 │  ├─ YES → 记录 "响应超时"
                 │  └─ NO  → 记录具体错误
                 ↓
                 递增 retryCount
                 ↓
                 ├─ retryCount <= 3?
                 │  ├─ YES → 继续重试
                 │  │        (500ms 后重新请求)
                 │  └─ NO  → 触发错误事件
                 │           → 显示错误提示
                 │           → 停止轮询
                 ↓
                 ┌──────────────────────┐
                 │ UI 显示：             │
                 │ ⚠️ 无法连接 Dashboard│
                 │ 请检查网络           │
                 └──────────────────────┘
```

---

## 总结

这个应用的核心逻辑遵循**事件驱动 + 轮询**的设计模式:

1. **轮询** 持续检测飞行状态 (每 500ms)
2. **事件** 一旦状态变化立即触发响应
3. **自动化** 整个过程完全自动化，用户无需干预
4. **可靠性** 完善的错误处理和重试机制
5. **响应性** 用户也可以手动控制流的启动/停止

